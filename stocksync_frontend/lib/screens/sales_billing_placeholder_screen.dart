import 'package:flutter/material.dart';

class SalesBillingPlaceholderScreen extends StatelessWidget {
  const SalesBillingPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Sales module ',
        textAlign: TextAlign.center,
      ),
    );
  }
}
